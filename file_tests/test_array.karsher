# initial array
arr = ["a", true, "bababa", ("ze" * 4 ), 1, 2.1, 3.]

arr = arr + [69, 420]

arr = arr * 2

# length of the array should be 18
arrlen = length(arr)

copy = []

count = 0

# copy the array one by one 
while (count < arrlen) {
    copy = copy + [arr[count]]
    count = count + 1
}

println(length(copy) == length(arr))
println(copy == arr)

# mutate array
arr[9]  = "Yolo"

# fancy list
i = 9
list = []

while(i !=0) {
    list = [i, list]
    i = i -1
}

# should print [1, [2, [3, [4, [5, [6, [7, [8, [9, []]]]]]]]]]
println(list)

# should print 2
println(length(list))